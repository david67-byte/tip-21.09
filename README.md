Практическое занятие 2. Петросян Давид ЭФБО-07-26 ФАЙЛ ПЗ-2 ПЕТРОСЯН.txt
ЗАДАНИЕ 1: Меняет местами значения двух переменных a и b только арифметическими операциями, без объявления третьей переменной.
ЗАДАНИЕ 2: Вычисляет произведение всех факториалов от 1! до n! за один проход. Каждый следующий факториал получается из предыдущего по формуле i! = (i-1)! * i, что исключает вложенный цикл
ЗАДАНИЕ 3: Заменяет конструкцию if (A == 0) B = 2; else B = 1; одним выражением без условных операторов
решено с помощью алиса ai и visual studio
